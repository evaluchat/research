# Research Catalog — Update Log

## 2026-08-13
* **Housekeeping**: Removed the catalog-wide `templates/` directory (evidence skeletons live on each method version as `evidence-template/`). Renamed `log.md` to `CHANGELOG.md`.
* **Method terminology**: Catalog objects are `type: Method` with YAML `levers:` (not Apparatus / knobs). Instance dirs moved from `apparatus/<id>/` to `methods/<id>/`; evidence lives only at `methods/<id>/evidence/`; the top-level `evidence/` registry is gone. `contribution-ladder` moved to `governance/` beside the review protocol. `methods/apparatus.en.md` folded into [shared evidence roles](governance/evidence-roles.en.md) plus the Essays method spec. Each method version pins an `evidence-template/`. README is the two-repo + chain front door; `build_site.py` renders it as the gh-pages landing; `index.md` stays the OKF section listing.
* **Authors required on Theory and Finding**: Catalog convention + `okf_lint` now require a non-empty `authors` list (each entry needs `name`). CI `okf-lint` runs on PRs/pushes to `main` only. See [design](docs/superpowers/specs/2026-08-13-theory-finding-authors-design.md).
* **Agent contract**: Merged the consumer (`AGENT.md`) and contributor (`AGENTS.md`) contracts into a single [AGENTS.md](AGENTS.md), the industry-standard filename. Deleted `AGENT.md`; README and CONTRIBUTING reserved-file list updated. Aligned the contract with the research-evidence programme: agents traverse question → protocol → ethics → method → evidence → review → finding; AI may structure work but not decide ethics, interpretation, or publication status; no claim stronger than its evidence and review.

## 2026-08-11
* **Blocked dependency and public feature request**: Marked the [Revision-tracking validity method](methods/revision-tracking-validity/revision-tracking-validity.en.md) as `implementation_status: blocked` because Canvas lacks a server-side, auditable assignment-group condition lever for random immutable profiles. Opened [feature request #1](https://github.com/evaluchat/research/issues/1) for the reusable `experimental-assignment-condition` capability and added an explicit public trail from synthetic acceptance through independent replication. No Canvas implementation or participant study has begun.
* **Research question and method specification**: Added [Revision-tracking validity](theory/revision-tracking-validity-question.en.md), an open theory question that distinguishes random allocation, recorded Canvas events, and unsupported authorship claims. Added the draft, assessor-blind [Revision-tracking validity method](methods/revision-tracking-validity/revision-tracking-validity.en.md), its evidence collection, and registry entries. The design specifies a reusable `experimental-assignment-condition` capability, condition-neutral metrics, privacy and grading safeguards, preregistration, reproducibility, and failure conditions; it implements no detector and contains no student data.
* **Research question and method specification**: Added [AI assignment stress-test validity](theory/ai-assignment-stress-test-validity.en.md), an open assessment-validity question grounded in an explicit interpretation/use argument, and linked it to the draft, teacher-side [AI Assignment Stress Test method](methods/ai-assignment-stress-test/ai-assignment-stress-test.en.md). Added its evidence collection and catalog index entries; no Canvas code, student data, or AI-use detection is included.
* **Method contract (public-beta runtime)**: `methods/ai-assisted-essay/ai-assisted-essay.en.md` updated from the rev-2 draft to the public-beta runtime contract — `min_canvas_version` (0.5.9), `required_capabilities`, typed lever definitions (`ai_assistance`, `ai_canvas_actions`, `drafting_gate`, `threshold`, `tracking`) with defaults/requires/ranges, five immutable profiles (`canonical-constrained-dialogue`, `gate-off`, `ai-off`, `canvas-actions-off`, `tracking-off`), provenance + `catalog_urls`. Methods index gains the Min Canvas column and profile/fixture note. `CONTRIBUTING.md` type vocabulary gains `Method` plus method PR rules (immutable IDs/versions, no executable deployment code). Synthetic fixtures under `methods/ai-assisted-essay/fixtures/` (profile matrix + tracking event shapes; no real participant data).

## 2026-08-10
* **Method structure (rev 2)**: Added published method dirs — `methods/ai-assisted-essay/ai-assisted-essay.en.md` (type: Method, spec as frontmatter: version 0.1.0, min_platform 0.5.9, research_questions, roles, levers, telemetry, sources) + methods index registry + `methods/ai-assisted-essay/evidence/index.md` (one evidence collection per method — structural). Evidence lives under `methods/<id>/evidence/`; CONTRIBUTING evidence-contribution path updated; root index lists Methods.

## 2026-08-09
* **Addition**: Added instrument-view notes later folded into [shared evidence roles](governance/evidence-roles.en.md) and the Essays method spec: dialogic contribution as a measured quantity, process signals at the abstraction level (no telemetry blueprints), the threshold as a research variable, measurement/observation/evidence separation, provenance `method: {id, version, levers, canvas}`.
* **Templates**: Evidence format is pinned per method version under `methods/<id>/evidence-template/`; shared roles live in governance.
* **Cross-links**: Theory files point at published methods and (via resource URL) the knowledge repo's `research-method` concept.
